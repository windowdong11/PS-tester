#include <bits/stdc++.h>
#define ONLINE_JUDGE
#ifndef ONLINE_JUDGE
#include "test/TestCase.h"
#endif
#define X first
#define Y second
using namespace std;


void solve(){
}

void initGlobal(){
}

int main()
{
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  #ifndef ONLINE_JUDGE
  runTests(solve, initGlobal, generate_TCNames(10));
  #else
  solve();
  #endif
  return 0;
}